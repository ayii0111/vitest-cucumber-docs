import Layout from '../layouts/Layout.astro';

<Layout title="vitest-cucumber - upcoming">
	# <i class="lni lni-calendar" style="position : relative; margin-left : 5px; top : 4px"></i> Upcoming

    I'm worjing on **vitest-cucumber** on many features.
    I have several ideas that I will integrate on the project.

    | feature | description |
    |--------------|-----------|
    | check first step | `Scenario` should start with `Given` 
    | step order | `When` must be after `Given`, `Then` after `When` etc |
    | alias | Add Gherkin `Example`, `Scenarios` and `*` alias |
</Layout>


